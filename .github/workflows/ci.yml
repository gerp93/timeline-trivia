# Build+vet gate — logic lives in gerp93/KVG_Standards' ci-go.yml, shared
# with gameshell-framework and card-judge.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: gerp93/KVG_Standards/.github/workflows/ci-go.yml@main
    with:
      working_directory: src
