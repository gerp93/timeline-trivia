# Per-game deployment config for card-timeline.
# Sourced by the gameshell-deploy scripts. See gameshell-deploy/examples/deploy.conf.

APP_NAME=card-timeline
ENV_PREFIX=CARD_TIMELINE
DB_NAME=CARD_TIMELINE
HTTP_PORT=2016
GIT_REPO=gerp93/card-timeline
GIT_UPSTREAM=
