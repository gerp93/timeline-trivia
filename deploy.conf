# Per-game deployment config for timeline-trivia.
# Sourced by the gameshell-deploy scripts. See gameshell-deploy/examples/deploy.conf.

APP_NAME=timeline-trivia
ENV_PREFIX=TIMELINE_TRIVIA
DB_NAME=TIMELINE_TRIVIA
HTTP_PORT=2016
GIT_REPO=gerp93/timeline-trivia
GIT_UPSTREAM=
